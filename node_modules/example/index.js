// @ts-check

import something from "./other.js"

/**
 * @param {string} toLog - Example string parameter
 * @returns {string} Passes back a string after logging
 */
export default function example(toLog) {
  console.log(toLog)
  return toLog
}
